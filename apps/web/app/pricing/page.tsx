import Link from 'next/link';
import { Check } from 'lucide-react';
import { MarketingNav } from '@/components/marketing-nav';
const features = ['Manual trade entry and CSV imports', 'Accounts, executions, tags, and playbooks', 'Dashboard, calendar, journal, and reports', 'Progress rules and daily reviews'];
export default function PricingPage() { return <><MarketingNav/><main className="shell py-20"><div className="mx-auto max-w-2xl text-center"><p className="eyebrow">Founding release</p><h1 className="mt-4 text-4xl font-semibold">Start with the complete foundation—for free.</h1><p className="mt-5 text-lg leading-8 text-muted">We are listening before we monetize. The early product stays free while we build it with traders.</p></div><div className="mx-auto mt-12 max-w-lg panel p-7 shadow-glow"><div className="flex items-start justify-between"><div><p className="text-xl font-semibold">Founding access</p><p className="mt-1 text-sm text-muted">For independent traders</p></div><p className="text-3xl font-semibold">$0<span className="text-sm font-normal text-muted"> / month</span></p></div><ul className="mt-7 grid gap-3">{features.map((feature) => <li className="flex gap-3 text-sm text-slate-300" key={feature}><Check className="size-5 shrink-0 text-mint"/>{feature}</li>)}</ul><Link href="/dashboard" className="button-primary mt-8 w-full">Join the founding release</Link><p className="mt-4 text-center text-xs text-muted">Future paid plans will be announced transparently and priced fairly.</p></div></main></>; }
