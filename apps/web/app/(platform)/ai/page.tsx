import { BrainCircuit, LockKeyhole, ShieldCheck } from 'lucide-react';
export default function AiPage() { return <div className="mx-auto max-w-4xl"><p className="eyebrow">Lab AI</p><h1 className="mt-1 text-3xl font-semibold">A coach that is grounded in your work.</h1><section className="panel mt-8 p-7"><div className="flex gap-4"><span className="grid size-11 shrink-0 place-items-center rounded-xl bg-teal/10 text-teal"><BrainCircuit className="size-5"/></span><div><h2 className="font-semibold">AI is intentionally disabled until a provider is approved.</h2><p className="mt-2 leading-7 text-muted">When enabled, every request is scoped to the current workspace, carries data provenance, respects role permissions, and includes clear feedback and opt-out controls. No financial advice or automatic execution is permitted.</p></div></div><div className="mt-6 grid gap-3 sm:grid-cols-2"><div className="rounded-xl border border-line bg-white/[.02] p-4"><ShieldCheck className="size-4 text-mint"/><p className="mt-3 font-semibold">Private context</p><p className="mt-1 text-sm text-muted">Only authorized workspace data is retrieved.</p></div><div className="rounded-xl border border-line bg-white/[.02] p-4"><LockKeyhole className="size-4 text-mint"/><p className="mt-3 font-semibold">Human control</p><p className="mt-1 text-sm text-muted">Suggestions are reviewable and never execute trades.</p></div></div></section></div>; }
