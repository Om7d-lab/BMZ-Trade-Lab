import { KeyRound, Languages, Shield, UserRound } from 'lucide-react';
export default function SettingsPage() { return <div className="mx-auto max-w-4xl"><p className="eyebrow">Workspace controls</p><h1 className="mt-1 text-3xl font-semibold">Settings</h1><div className="mt-8 grid gap-5">{([[UserRound, 'Profile & preferences', 'Name, timezone, base currency, and default dashboard view.'], [Languages, 'Language & layout', 'English is the primary locale; Persian and Arabic render RTL.'], [Shield, 'Security & privacy', 'Manage active sessions, exports, retention, and workspace roles.'], [KeyRound, 'Connected providers', 'Broker, market data, email, AI, storage, and billing credentials are securely configured by administrators.']] as [typeof UserRound, string, string][]).map(([Icon, title, text]) => <button className="panel flex items-center gap-4 p-5 text-left transition hover:border-teal/50" key={String(title)}><span className="grid size-10 place-items-center rounded-xl bg-teal/10 text-teal"><Icon className="size-5"/></span><span><span className="block font-semibold">{title}</span><span className="mt-1 block text-sm text-muted">{text}</span></span></button>)}</div></div>; }
