'use client';
import Link from 'next/link';
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { ArrowUpRight, CalendarDays, Eye, EyeOff, MoreHorizontal, Plus } from 'lucide-react';
import { calendar, equity, trades } from '@/lib/demo-data';
import { cn } from '@/lib/cn';
import { useState } from 'react';
const currency = new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', maximumFractionDigits: 0 });
function Metric({ label, value, change, tone = 'good' }: { label: string; value: string; change?: string; tone?: 'good' | 'bad' }) { return <div className="panel p-5"><p className="metric-label">{label}</p><div className="mt-3 flex items-end justify-between gap-2"><p className="text-2xl font-semibold tracking-tight">{value}</p>{change && <span className={cn('text-xs font-bold', tone === 'good' ? 'text-mint' : 'text-danger')}>{change}</span>}</div></div>; }
export default function DashboardPage() { const [privateMode, setPrivateMode] = useState(false); const money = (value: number) => privateMode ? '••••••' : currency.format(value); return <div className="mx-auto max-w-7xl"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="eyebrow">Your command center</p><h1 className="mt-1 text-3xl font-semibold">Good morning, Avery.</h1><p className="mt-2 text-sm text-muted">Friday, September 20 · Your review is one step away.</p></div><div className="flex gap-2"><button onClick={() => setPrivateMode(!privateMode)} className="button-secondary"><>{privateMode ? <Eye className="size-4"/> : <EyeOff className="size-4"/>}</>Privacy</button><Link href="/trades?new=1" className="button-primary"><Plus className="size-4"/>Add trade</Link></div></div><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Net P&L" value={money(4820)} change="+12.8%"/><Metric label="Win rate" value="62.5%" change="+4.2%"/><Metric label="Profit factor" value="1.84" change="+0.16"/><Metric label="Max drawdown" value={money(-640)} change="Within guardrail" tone="good"/></div><div className="mt-5 grid gap-5 xl:grid-cols-[1.55fr_1fr]"><section className="panel p-5"><div className="flex items-center justify-between"><div><p className="font-semibold">Equity curve</p><p className="mt-1 text-sm text-muted">Net P&L · last 5 sessions</p></div><button aria-label="More equity options"><MoreHorizontal className="size-5 text-muted"/></button></div><div className="mt-5 h-72"><ResponsiveContainer width="100%" height="100%"><AreaChart data={equity}><defs><linearGradient id="equity" x1="0" x2="0" y1="0" y2="1"><stop offset="0" stopColor="#34D399" stopOpacity={.35}/><stop offset="1" stopColor="#34D399" stopOpacity={0}/></linearGradient></defs><CartesianGrid stroke="#1B2C3E" vertical={false}/><XAxis dataKey="day" tickLine={false} axisLine={false} tick={{ fill: '#91A5BB', fontSize: 12 }}/><YAxis hide/><Tooltip contentStyle={{ background: '#0C1827', border: '1px solid #1B2C3E', borderRadius: 12 }} formatter={(v) => currency.format(Number(v))}/><Area type="monotone" dataKey="equity" stroke="#34D399" strokeWidth={3} fill="url(#equity)"/></AreaChart></ResponsiveContainer></div></section><section className="panel p-5"><div className="flex items-center justify-between"><div><p className="font-semibold">Today&apos;s process</p><p className="mt-1 text-sm text-muted">2 of 3 stages complete</p></div><span className="rounded-full bg-mint/15 px-2.5 py-1 text-xs font-bold text-mint">67%</span></div><div className="mt-6 grid gap-3">{[['Prepare', 'Pre-market plan saved', true], ['Trade', 'Stay inside your defined hours', true], ['Reflect', 'Review each trade before close', false]].map(([stage, rule, done]) => <div className="flex items-center gap-3 rounded-xl border border-line bg-white/[.02] p-3" key={String(stage)}><span className={cn('grid size-6 place-items-center rounded-full border text-xs', done ? 'border-mint bg-mint text-ink' : 'border-slate-600 text-muted')}>{done ? '✓' : '3'}</span><div><p className="text-sm font-medium">{stage}</p><p className="text-xs text-muted">{rule}</p></div></div>)}</div><Link href="/progress" className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-teal hover:text-mint">Continue my day <ArrowUpRight className="size-4"/></Link></section></div><div className="mt-5 grid gap-5 xl:grid-cols-[1.2fr_.8fr]"><section className="panel p-5"><div className="flex items-center justify-between"><div><p className="font-semibold">Recent trades</p><p className="mt-1 text-sm text-muted">Your latest executions and reviews</p></div><Link href="/trades" className="text-sm font-semibold text-teal">View all</Link></div><div className="mt-4 divide-y divide-line">{trades.slice(0,4).map((trade) => <Link href={`/trades/${trade.id}`} key={trade.id} className="flex items-center gap-3 py-3 hover:bg-white/[.02]"><span className={cn('grid size-9 place-items-center rounded-lg text-xs font-bold', trade.side === 'Long' ? 'bg-mint/15 text-mint' : 'bg-danger/15 text-danger')}>{trade.side[0]}</span><div className="min-w-0 flex-1"><p className="font-medium">{trade.symbol} <span className="text-xs font-normal text-muted">· {trade.playbook}</span></p><p className="text-xs text-muted">{trade.openedAt}</p></div><div className="text-right"><p className={cn('font-semibold', trade.pnl > 0 ? 'text-mint' : 'text-danger')}>{money(trade.pnl)}</p><p className="text-xs text-muted">{trade.reviewed ? 'Reviewed' : 'Needs review'}</p></div></Link>)}</div></section><section className="panel p-5"><div className="flex items-center justify-between"><div><p className="font-semibold">September</p><p className="mt-1 text-sm text-muted">Daily net P&L</p></div><CalendarDays className="size-5 text-teal"/></div><div className="mt-5 grid grid-cols-7 gap-2 text-center text-[10px] text-slate-500">{['M','T','W','T','F','S','S'].map((d, i) => <span key={d+i}>{d}</span>)}{calendar.map((value, i) => <span key={i} title={value ? currency.format(value) : 'No trading'} className={cn('grid aspect-square place-items-center rounded-md', value > 0 && 'bg-mint/25 text-mint', value < 0 && 'bg-danger/20 text-danger', value === 0 && 'bg-white/[.03]')}>{i + 1}</span>)}</div><Link href="/journal" className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-teal">Open day view <ArrowUpRight className="size-4"/></Link></section></div></div>; }
