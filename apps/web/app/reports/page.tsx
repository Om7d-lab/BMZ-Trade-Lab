'use client';
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { Download, Filter, Plus } from 'lucide-react';
const data = [{ name: 'Opening Drive', pnl: 2120, win: 67 }, { name: 'VWAP Reclaim', pnl: 1480, win: 61 }, { name: 'London Sweep', pnl: 960, win: 58 }, { name: 'Breakout', pnl: 260, win: 43 }];
export default function ReportsPage() { return <div className="mx-auto max-w-7xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Performance intelligence</p><h1 className="mt-1 text-3xl font-semibold">Reports</h1><p className="mt-2 text-sm text-muted">Interrogate the patterns behind your results.</p></div><div className="flex gap-2"><button className="button-secondary"><Filter className="size-4"/>All accounts · 30 days</button><button className="button-secondary"><Download className="size-4"/>Export</button></div></div><div className="mt-8 grid gap-5 xl:grid-cols-[1.5fr_.7fr]"><section className="panel p-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Net P&L by playbook</h2><p className="mt-1 text-sm text-muted">Where did your edge actually show up?</p></div><button className="button-secondary py-2"><Plus className="size-4"/>Metric</button></div><div className="mt-6 h-80"><ResponsiveContainer width="100%" height="100%"><BarChart data={data}><CartesianGrid vertical={false} stroke="#1B2C3E"/><XAxis dataKey="name" tickLine={false} axisLine={false} tick={{fill:'#91A5BB',fontSize:11}}/><YAxis hide/><Tooltip cursor={{fill:'rgba(45,212,191,.06)'}} contentStyle={{background:'#0C1827',border:'1px solid #1B2C3E',borderRadius:12}}/><Bar dataKey="pnl" fill="#2DD4BF" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div></section><section className="panel p-5"><h2 className="font-semibold">Quick read</h2><div className="mt-5 grid gap-4">{[['Best playbook','Opening Drive','+$2,120'],['Most disciplined day','Friday','94% rules kept'],['Behavior leak','FOMO','-$310 across 2 trades']].map(([label, value, stat]) => <div className="rounded-xl border border-line bg-white/[.025] p-4" key={label}><p className="metric-label">{label}</p><p className="mt-2 font-semibold">{value}</p><p className="mt-1 text-sm text-muted">{stat}</p></div>)}</div></section></div><section className="panel mt-5 overflow-hidden"><div className="border-b border-line p-5"><h2 className="font-semibold">Playbook scorecard</h2><p className="mt-1 text-sm text-muted">Sort or cross-filter this table by symbol, session, day, tag, risk, and duration.</p></div><div className="overflow-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="bg-white/[.02] text-xs uppercase text-muted"><tr>{['Playbook','Trade count','Win rate','Net P&L','Profit factor','Average R'].map((h) => <th key={h} className="px-5 py-3 font-medium">{h}</th>)}</tr></thead><tbody>{data.map((row, i) => <tr key={row.name} className="border-t border-line"><td className="px-5 py-4 font-semibold">{row.name}</td><td className="px-5 py-4 text-muted">{[18,14,9,12][i]}</td><td className="px-5 py-4 text-mint">{row.win}%</td><td className="px-5 py-4 text-mint">+${row.pnl.toLocaleString()}</td><td className="px-5 py-4">{[2.1,1.8,1.6,.94][i]}</td><td className="px-5 py-4">{[1.42,1.1,.84,-.08][i]}R</td></tr>)}</tbody></table></div></section></div>; }
