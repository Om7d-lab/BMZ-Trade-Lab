import { CheckCircle2, MoreHorizontal, Plus, Target } from 'lucide-react';
const playbooks = [{ name: 'Opening Drive', color: 'bg-mint', trades: 18, win: '67%', pnl: '+$2,120', rules: ['Premarket range defined', 'First pullback only', 'Risk fixed before entry'] }, { name: 'VWAP Reclaim', color: 'bg-teal', trades: 14, win: '61%', pnl: '+$1,480', rules: ['Trend context confirmed', 'Reclaim closes with volume', 'No chase entry'] }, { name: 'London Sweep', color: 'bg-indigo-400', trades: 9, win: '58%', pnl: '+$960', rules: ['Liquidity sweep visible', 'Displacement confirmed', 'Target at opposing liquidity'] }];
export default function PlaybooksPage() { return <div className="mx-auto max-w-7xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Your repeatable edge</p><h1 className="mt-1 text-3xl font-semibold">Playbooks</h1><p className="mt-2 text-sm text-muted">Define your setups, attach them to trades, and let results earn your conviction.</p></div><button className="button-primary"><Plus className="size-4"/>Create playbook</button></div><div className="mt-8 grid gap-5 lg:grid-cols-3">{playbooks.map((book) => <article className="panel p-5" key={book.name}><div className="flex items-start justify-between"><span className={`size-3 rounded-full ${book.color}`}/><button aria-label={`More options for ${book.name}`}><MoreHorizontal className="size-5 text-muted"/></button></div><h2 className="mt-5 text-xl font-semibold">{book.name}</h2><div className="mt-5 grid grid-cols-3 gap-3 border-y border-line py-4 text-center"><div><p className="metric-label">Trades</p><p className="mt-1 font-semibold">{book.trades}</p></div><div><p className="metric-label">Win rate</p><p className="mt-1 font-semibold text-mint">{book.win}</p></div><div><p className="metric-label">Net P&L</p><p className="mt-1 font-semibold text-mint">{book.pnl}</p></div></div><h3 className="mt-5 text-sm font-semibold">Entry criteria</h3><ul className="mt-3 grid gap-2">{book.rules.map((rule) => <li className="flex items-center gap-2 text-sm text-muted" key={rule}><CheckCircle2 className="size-4 text-teal"/>{rule}</li>)}</ul><button className="button-secondary mt-6 w-full"><Target className="size-4"/>Review setup</button></article>)}</div></div>; }
