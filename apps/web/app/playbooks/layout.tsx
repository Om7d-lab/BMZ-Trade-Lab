import { PlatformShell } from '@/components/platform-shell';
export default function PlaybooksLayout({ children }: { children: React.ReactNode }) { return <PlatformShell>{children}</PlatformShell>; }
